

WIP...


just to remember

initial adding of submodule in already existing repository:

```
git submodule add https://git.boddenberg.it/croni

```

how to fetch repository and submodule from scratch:

```
git clone https://your.git.server/foo.git
cd foo
git submodule init
git submodule update
```

